# Social Cup Frontend Environment Variables

# Base URL of the backend API (backend/, not the old server/). Vite only
# exposes vars prefixed VITE_ to client code.
VITE_API_URL="http://localhost:5000/api"
